List all the dependencies and setting up an installer

set keymap Description for the new keymap and check if there are any conflict

Create a plugin that manage tmux session, every time I open a project it search for the corresponding session an attach to it, otherwise it create a new session, It should not be necessary to be in a tmux session, just launch nvim .

Check if there is some available optimization in plugin loading
